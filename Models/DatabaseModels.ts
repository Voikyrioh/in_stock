export enum Tables {
    RETARDEDS = "retardeds",
    PRODUCTS = "products",
    SOURCES = "sources",
    RETARDED_PRODUCTS = "retarded_products",
}

export interface TableAttribute {
    id: number;
}
